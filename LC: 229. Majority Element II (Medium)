LC: 229. Majority Element II (Medium)

class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        res= []
        freq= {}
        n= len(nums)

        for num in nums:
            freq[num]= freq.get(num, 0)+1
            if freq[num]> n//3 and num not in res:
                res.append(num)
        
        return res
        

