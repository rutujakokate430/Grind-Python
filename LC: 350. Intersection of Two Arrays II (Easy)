LC: 350. Intersection of Two Arrays II (Easy)

class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        c= Counter(nums1)
        output=[]
        for num in nums2:
            if c[num]>0:
                output.append(num)
                c[num]-=1
        return output

        
